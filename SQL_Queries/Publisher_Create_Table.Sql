# SQL Script Used To Create Publisher Table

CREATE TABLE `CS_631`.`PUBLISHER` ( `PUBLISHERID` VARCHAR(25) NOT NULL , `PUBNAME` VARCHAR(50) NOT NULL , `ADDRESS` VARCHAR(100) NOT NULL , PRIMARY KEY (`PUBLISHERID`));
